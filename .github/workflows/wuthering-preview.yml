name: Wuthering Preview

on:
  push:
    paths:
      - "apps/wuthering-waves-overwolf/manifest.json"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - run: bun install
      - name: Build Overwolf app
        run: bunx turbo run build --filter=wuthering-waves-overwolf...
      - name: Release Overwolf app
        env:
          OW_CLI_EMAIL: ${{ secrets.OW_CLI_EMAIL }}
          OW_CLI_API_KEY: ${{ secrets.OW_CLI_API_KEY }}
          PREVIEW_ACCESS_CHANNEL_ID: 870
        run: bunx turbo run release --filter=wuthering-waves-overwolf...
      - name: Promote tested build to production
        env:
          OW_CLI_EMAIL: ${{ secrets.OW_CLI_EMAIL }}
          OW_CLI_API_KEY: ${{ secrets.OW_CLI_API_KEY }}
          OW_APP_ID: gjohaodckfkkodlmmmmeifkdkifddegkleppngad
          PREVIEW_ACCESS_CHANNEL_NAME: preview-access
        run: bunx turbo run promote --filter=wuthering-waves-overwolf
